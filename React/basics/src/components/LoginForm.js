import React from "react"

const LoginForm = () => {
    return (
        <>
            <input type="password" />
            <input type="text"/>
            <button>Login</button>
        </>
    )
}

export default LoginForm